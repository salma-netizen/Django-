apiVersion: v1
kind: Pod
metadata:
  name: redis
spec:
  containers:
  - name: redis
    image: redis
    volumeMounts:
    - name: redis-storage
      mountPath: /data/redis  # ✅ fixed typo: "moumtPath" → "mountPath"
  volumes:
  - name: redis-storage
    emptyDir: {}  # ✅ added volume type